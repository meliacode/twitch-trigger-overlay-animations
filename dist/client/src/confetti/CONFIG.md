# Confetti configuration

- `count`: number of confetti pieces per trigger.
- `duration`: fall duration in milliseconds.
- `delay`: time between pieces in milliseconds.
- `colors`: confetti palette.
- `TWITCH.command`: default chat command is `!confetti`.
