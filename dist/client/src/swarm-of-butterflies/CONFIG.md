# Swarm of Butterflies configuration

- `count`: number of butterflies released per trigger.
- `duration`: flight length in milliseconds.
- `delay`: time between butterflies in milliseconds.
- `minSize` and `maxSize`: emoji size range in pixels.
- `colors`: display palette (emoji rendering can vary by operating system).
- `TWITCH.command`: default chat command is `!butterflies`.
