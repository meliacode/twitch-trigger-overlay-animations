# Send Love configuration

- `count`: number of hearts per trigger.
- `duration`: travel time in milliseconds.
- `delay`: time between new hearts in milliseconds.
- `minSize` and `maxSize`: heart size range in pixels.
- `TWITCH.command`: default chat command is `!love`.
